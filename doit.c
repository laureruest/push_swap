/*
 * Cabecera
 */

#include "t_list.h"
#include "libft.h"

char *doit(t_list *a, t_list *b, int *error)
{
	char	*salida;

	salida = malloc(sizeof(char));
	if (salida)
		*salida = '\0';
	return (salida);
}

